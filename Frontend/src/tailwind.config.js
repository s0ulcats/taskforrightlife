module.exports = {
    darkMode: 'class', // Позволяет использовать темную тему через класс 'dark'
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Указывает, какие файлы Tailwind должен обрабатывать
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };